# Complex / relational-learning / 2026-09-01

**Trunk:** Custom authenticated sets can inform *both* sklearn and Spiral on structure-and-substance tasks; UCI match stays a separate floor.

**Checkpoint:** draft — pair agreed the hinge; first sheet not yet locked.

## Slots filled

| Slot | Fill |
|------|------|
| Seed | Complement first; surpass only after Verified complement |
| Associate | Poetry Tree (signed trees as rows) |
| Bound | SRT prune / decoration cut |
| Grade | Grandma meter + Test_Cases + local hard-example DB |
| Birth | AIS-Standard (private incubation) |
| Floor | Spiral-comparative-eval classic baseline |
| Shield | E_shield; no prestige feature; no baked meter score |
| Remember | This file + campaign doc + EVAL_LINK |
| Stamp | Codex + eval commits of 2026-09-01 |
| Checkpoint | Human — still draft |

## Empty on purpose

Market treasury response. Agent-Hub deploy. Discord. Any mint.

## First intended emissions (order)

1. A sheet of `trunk_membership` rows from already-signed trees (kept vs pruned/swapped).
2. A sheet of `claim_substance` rows from `grandmas-wisdom/examples/Test_Cases.md` and the hard-example DB.
3. Public task cards in comparative-eval once the splits exist.
4. Only then: a child method (e.g. a tiny `extensions/relational_features.py` in SRT) that *reads* those columns — not a new cathedral.

## Kill conditions for the trunk

- The spiral candidate only wins with smuggled labels.
- The set is not authenticated (no path, no mark).
- We announce surpass before complement is Verified.

## Grandma on this complex itself

Tenable as a *composition plan*.  
Not yet supported as a *measured improvement*.  
Needs qualification: "frameworks that make frameworks" is true only while an emission is a file another person could rerun.
